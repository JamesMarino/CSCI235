SQL> -- Question 1
SQL> SELECT DNAME FROM DEPTLOC WHERE CITY IN ('BOSTON', 'DALLAS')
MINUS (
SELECT DNAME FROM DEPTLOC WHERE CITY = 'DALLAS'
INTERSECT
SELECT DNAME FROM DEPTLOC WHERE CITY = 'BOSTON'
);

DNAME        
--------------
OPERATIONS    

SQL> -- Question 2
SQL> SELECT DNAME FROM DEPARTMENT
MINUS
SELECT DNAME FROM EMPLOYEE;

DNAME        
--------------
IT            

SQL> -- Question 3
SQL> SELECT CITY FROM DEPTLOC WHERE DNAME = 'SALES'
MINUS
SELECT CITY FROM DEPTLOC WHERE DNAME = 'TRANSPORT';

CITY        
-------------
BOSTON       
CHICAGO      
DALLAS       
LOS ANGELES  
PARIS        

SQL> -- Question 4
SQL> SELECT DISTINCT DNAME
FROM DEPTLOC DL
WHERE DL.DNAME IN ('SALES', 'TRANSPORT', 'RESEARCH')
AND NOT EXISTS (
SELECT CITY FROM DEPTLOC
MINUS
SELECT CITY FROM DEPTLOC
WHERE DNAME = DL.DNAME
);

no rows selected


SQL> SPOOL OFF;
